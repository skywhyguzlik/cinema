<Page x:Class="cinema.Views.MoviesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:cinema.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
     Title="MoviesPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#F0F0F0" Padding="10" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="SearchTextBox" Width="200" TextChanged="SearchTextBox_TextChanged" Margin="0,0,20,0"/>
                <TextBlock Text="Сортировать:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="SortComboBox" Width="120" SelectionChanged="SortComboBox_SelectionChanged">
                    <ComboBoxItem Content="Названию"/>
                    <ComboBoxItem Content="Рейтингу"/>
                    <ComboBoxItem Content="Дате выхода"/>
                </ComboBox>
                <Button x:Name="SortDirectionButton" Content="↑" Width="30" Margin="5,0" Click="SortDirectionButton_Click"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="MoviesPanel" Margin="20"/>
        </ScrollViewer>
    </Grid>
</Page>
