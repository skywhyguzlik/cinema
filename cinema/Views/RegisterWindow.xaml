<Window x:Class="cinema.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cinema.Views"
        mc:Ignorable="d"
 Title="Регистрация" Height="350" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Регистрация" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" Padding="5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Пароль:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10" Padding="5"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Полное имя:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox x:Name="FullNameTextBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,10" Padding="5"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Дата рождения:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <DatePicker x:Name="BirthDatePicker" Grid.Row="4" Grid.Column="1" Margin="0,0,0,20"/>

        <Button x:Name="RegisterBtn" Grid.Row="5" Grid.ColumnSpan="2" Content="Зарегистрироваться" Width="150" Height="30" Click="RegisterBtn_Click"/>
        <TextBlock x:Name="ErrorText" Grid.Row="6" Grid.ColumnSpan="2" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</Window>
