<Page x:Class="cinema.Views.TicketPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:cinema.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
         Title="Оформление билета">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Подтверждение билета" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <TextBlock x:Name="MovieText" Grid.Row="1" Margin="0,0,0,5"/>
        <TextBlock x:Name="HallText" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBlock x:Name="DateTimeText" Grid.Row="3" Margin="0,0,0,5"/>
        <TextBlock x:Name="SeatText" Grid.Row="4" Margin="0,0,0,20"/>
        <TextBlock x:Name="PriceText" Grid.Row="5" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Подтвердить" Width="100" Height="30" Margin="5" Click="Confirm_Click"/>
            <Button Content="Отмена" Width="100" Height="30" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Page>
